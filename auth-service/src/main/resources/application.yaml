spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:http://localhost:8888"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
server:
  servlet:
    context-path: /api/v1
